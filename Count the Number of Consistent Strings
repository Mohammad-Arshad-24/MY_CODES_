class Solution {
    public int countConsistentStrings(String allowed, String[] words) {
        int c=0;
        for(String w : words){
            if(w.matches("[" + allowed +"]+")){
                c++;
            }
        }
        return c;
    }
}
