class Solution {
    public int minimumSum(int num) {
        String s=String.valueOf(num);
        char [] r=s.toCharArray();
        Arrays.sort(r);
        int a=((int)r[0]-'0')*10 + ((int)r[2])-'0';
        int b=((int)r[1]-'0')*10 + ((int)r[3])-'0';
        return a+b;
    }
}
