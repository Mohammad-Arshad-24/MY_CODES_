arr=[1,2,0,4,3,0,5,0] #can also take the input of the array
n = len(arr)
for j in range(n):
    for i in range(n - 1):
        max = i + 1
        if arr[i]<arr[max]:
            temp = arr[i]
            arr[i] = arr[max]
            arr[max] = temp
print(arr)
# output is [5, 4, 3, 2, 1, 0, 0, 0]
