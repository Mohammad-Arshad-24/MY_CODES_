import math
class Solution:
    def gcdOfStrings(self, str1: str, str2: str) -> str:
        m=len(str1)
        n=len(str2)
        l=math.gcd(m,n)
        check_string=str1[:l]
        if(str1==check_string*(m//l) and str2==check_string*(n//l)):
            return check_string
        else:
            return ""
